import { Local } from "../environment/env";
import Notification from "../models/Notification";

export const sendNotification=async (req:any,res:any)=>{
    const {senderId,notification,receiverId}=req.body

    )
}